# Current task

WP20 — AI recommendations (complete). Geo Core `1.8.156`. Decision Cloud `0.10.0`.
Sprints 1–6 complete. Decision Cloud `0.15.0`.

Next: **Gate D** on the ReactWoo Local site. See `docs/architecture/gate-d.md`.
Decision Cloud lives at `wp-content/plugins/reactwoo-decision-cloud` (Node service, not a WP plugin).
Start with `npm start`, then pair from `/portal/#/sites/connect`.
