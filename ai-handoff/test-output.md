# Test output

> Human pastes local results here after Cursor’s pass. Keep only the failing command and the **last ~80 lines** of output.

## Command

```bash
# e.g. composer test, npm run package:zip, phpunit, wp-cli
```

## Output

```
(paste tail of stderr/stdout)
```

## Environment (optional)

- Site / branch:
- PHP / Node version:
- Plugin versions:
